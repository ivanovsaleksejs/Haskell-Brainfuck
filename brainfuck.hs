import Brainfuck.Brainfuck
import Prelude hiding ((+), (-), (>), (<), (*), init)

main = do
    run code
    run' code'
    run [(#), (*)]
    run' ",+."

code = [
        (>), (+), (>), (-), (<), (+), (+), (+), (+), (+), (+), (+), (+),
        (+), (+), (+), (+), (+), (+), (+), (+), (+), (+), (+), (+), (+),
        (+), (+), (+), (+), (+), (+), (+), (+), (+), (+), (+), (+), (+),
        (+), (+), (+), (+), (+), (+), (+), (+), (+), (+), (+), (+), (+),
        (+), (+), (+), (+), (+), (+), (+), (+), (+), (+), (+), (+), (+),
        (+), (+), (+), (+), (+), (+), (+), (+), (+), (+), (+), (*)
        ]

code' = ">+>-<+++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++."
